// src/components/nations/panel-helpers.ts
export { getErrorMessage, showNationError } from "./errors";

/** Cost to activate 24h protection (paid from nation vault). */
export const PROTECTION_COST = { wardog: 10, warcat: 10 };
